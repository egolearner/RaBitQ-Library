# RaBitQ Library

本文档定义 RaBitQ Library 跨平台支持范围中的统一术语。

## 平台

**AArch64 平台**：
具备 Advanced SIMD 能力的 64 位 ARM 运行环境，包括 macOS `arm64` 和 Linux `aarch64`；不包括 ARMv7、其他 32 位 ARM 环境及 ARM64EC。
_避免使用_：ARM 平台、ARM 支持

**原生架构产物**：
仅面向一个目标操作系统和处理器架构构建的库或可执行文件；不同处理器架构分别发布产物。
_避免使用_：跨架构二进制、Universal Binary

## 兼容性

**跨架构兼容**：
索引与编码数据可在受支持的 x86_64 和 AArch64 产物间交换，查询结果保持数值与语义等价；浮点中间结果及同分候选顺序不要求逐位一致。
_避免使用_：跨架构逐位一致
