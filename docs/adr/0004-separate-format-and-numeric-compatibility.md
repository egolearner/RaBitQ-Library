---
status: accepted
---

# 区分格式一致性与浮点数值等价

跨架构兼容要求编码打包、位转置、序列化元数据及索引中的整数编码逐位一致，以保证 x86_64 与 AArch64 可以互相加载索引。浮点内积和距离允许因 FMA 及归约顺序产生容差内差异，Top-K 使用距离容差和召回率验证，同分候选不要求 ID 顺序完全一致；这避免为了不影响语义的浮点末位差异牺牲 SIMD 性能。
