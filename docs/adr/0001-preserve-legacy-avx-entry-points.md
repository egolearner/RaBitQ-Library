---
status: accepted
---

# 保留历史 AVX 命名入口

`space.hpp` 中已有的七个 `*_avx` 函数继续作为跨架构兼容入口，以避免破坏现有调用方；通用调用链直接分派到 `*_avx2`、`*_avx512` 或 `*_neon` 后端，不经过这些兼容入口。AVX2、AVX-512 和 NEON 后端函数属于架构专属内部接口，AArch64 产物不提供 AVX 后端符号，新代码也不再增加模糊的 `*_avx` 通用名称。
