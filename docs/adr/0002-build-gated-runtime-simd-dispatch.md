---
status: accepted
---

# 构建期裁剪并在运行时选择 SIMD 后端

每个原生架构产物只编译与目标架构兼容的 SIMD 后端，进程静态初始化时根据实际 CPU 能力从已编译后端中选择最优实现，并建立不可变函数表。初始化过程不得抛异常、记录日志或依赖其他翻译单元的全局状态；当没有满足要求的生产后端时，函数表使用错误桩并在功能实际被调用时明确失败，不静默退回标量实现。这样可以避免在同一产物中引用不兼容的机器指令，同时让热路径只承担既有的函数指针调用成本。
